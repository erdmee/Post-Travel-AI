# Gemini API key (get one at https://aistudio.google.com/apikey)
GEMINI_API_KEY=

# Model name
LLM_MODEL=gemini-3-flash-preview

# Shared secret with NestJS backend (must match GPU_INTERNAL_TOKEN there)
GPU_INTERNAL_TOKEN=dev-internal-token
